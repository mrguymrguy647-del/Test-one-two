plugins {
    id 'com.android.application'
}

android {
    namespace 'com.blockcraft.game'
    compileSdk 34

    defaultConfig {
        applicationId 'com.blockcraft.game'
        minSdk 21
        targetSdk 34
        versionCode 2
        versionName '2.0'
    }

    // The game itself lives in ../../web so the browser version and the app share the same files.
    sourceSets {
        main {
            assets.srcDirs = ['../../web']
        }
    }

    buildTypes {
        release {
            minifyEnabled false
        }
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
    }
}
